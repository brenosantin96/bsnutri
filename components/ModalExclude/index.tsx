import styles from './styles.module.css'

type Props ={
    valueToRemove : string;
    id: number;
}

export const ModalExclude = ({valueToRemove, id}: Props) => {
    return (
        <div className={styles.container}>

        </div>
    )
}